const Task = () => {
    const addForm = function () {
        //* when Add task is clicked, replace it with the form
    };

    const addTask = function () {
        //* when add in the form is clicked, add the task to the list
    };
};
